git-版本控制系统

分为client与server端
server端：（如gitub）
在上面创建仓库。server端上的代码被称为origin。server端上的代码可以被任何人（有对应权限）连接克隆。是所有开发者共同开发可见的。
client：（使用者本机）
本机的一个代码版本管理仓库，通过本机仓库与远程地址建立关系。可以拉取远程代码更新本地或推送本地代码更新origin。仅属于单一开发者。


分支（branch）
分为3大类。
master（唯一）:开发期间极少碰这个分支。只有一个项目一个成熟的版本在dev完成后才会合并到master。通常会比实际线上代码旧几个版本。
dev（唯一）：实际上线分支，将代码上线都是基于这个分支来上线的。不同的人将测试无误的功能合并到这个分支。
xx功能开发分支（众多）：开发分支，单一功能的开发分支。代码经常被修改，测试无误后合并到dev。

分支同样在client与server对应存在。
server的分支对所有人可见。
而client的分支可以仅在本地存在，也可以与远程分支建立连接互相推送。

协议:
http,ftp,rsync,git(基于ssh)
克隆一个仓库一般有两个选项，https与git