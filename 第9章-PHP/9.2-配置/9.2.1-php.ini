9.2.1-php.ini
session.save_handler 这个参数用于指定Session的存储方式（实际上是指定了一个处理Session的句柄）。可以是files（文件存储，默认）, user( 用户自定义存储 )，或者其他的扩展方式（如memcache）。
在设置了session.save_handler=files的情况下，服务器端是以sess_{session_id}的命名方式来储存Session数据文件的