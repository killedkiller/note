controller-路由
actions-接收参数 调用相应的ps层
ps层-{
	打点
	校验格式化参数
	整理数据根据请求类型向不同data层请求所需数据
	对数据做错误验证
	整理拼接数据返回
}，
data层-{
	按照ps层需求，实现具体的数据逻辑（复数个原子操作）
	这层的逻辑实现应该排除了对请求类型的处理
}
dao层-{
	封装单个表的增删查改操作
	原子操作
}


action层和ps层都会有不同版本的接口
而data层和dao层不存在，所以会因改版而改变的逻辑不该放在这里面